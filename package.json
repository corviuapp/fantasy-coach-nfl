{
  "name": "fantasy-coach-nfl",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "serve -c serve.json -l 8080",
    "install": "npm install --prefix backend && npm install --prefix frontend"
  }
}